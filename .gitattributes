# gofmt 门禁跨平台一致性：GitHub Windows runner 默认 core.autocrlf=true，
# 会把 LF 源码 checkout 成 CRLF，gofmt -l 随即把全部 .go 判为未格式化。
# 强制 LF 落盘（仓库内 blob 本就是 LF，此文件只约束 checkout 形态）。
*.go text eol=lf
go.mod text eol=lf
go.sum text eol=lf
*.sh text eol=lf

# webui 前端源码与文档统一 LF：历史混行尾让首次修改的文件产生整文件 diff（行尾噪音）。
# 二进制资产（woff2/svg）不加 text 属性，保持二进制安全。
*.ts text eol=lf
*.tsx text eol=lf
*.css text eol=lf
*.md text eol=lf
*.yaml text eol=lf
